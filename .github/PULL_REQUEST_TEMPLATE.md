<!-- What this changes and why. Link the issue: "Fixes #N". -->

- [ ] `make test` passes
- [ ] `make lint` is clean (`make fmt` fixes it)
- [ ] If the render path changed: looked at `make demo-cold`, not just the warm `make demo`
- [ ] If behaviour changed: `doc/eqnav.txt` (and the README, where it covers it) updated
